/**
 * Methods for VUEX
 */

import {VS_SCREENS} from './store/constants';

const VuexMixin = {
    employ(context, Store) {
        let mixins = {
            /**
             * @override
             */

        };

        Object.assign(context, mixins);
    }
};

export default VuexMixin;